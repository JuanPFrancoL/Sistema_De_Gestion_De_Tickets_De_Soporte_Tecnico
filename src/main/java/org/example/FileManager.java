package org.example;

import java.io.File;

public class FileManager {
    File file;
    SimpleList simpleList = new SimpleList();
    public void readFile(File file) {
        try {
            
        }
    }
}
